# =============================================================================
# API Server — Environment Variables
# =============================================================================
# Copy this file to .env inside artifacts/api-server/ to override defaults.
# The server runs on PORT 8080 by default — no changes required locally.
# =============================================================================

# Port the Express API server listens on.
PORT=8080

# Node environment. Set to "production" for deployed builds.
NODE_ENV=development
